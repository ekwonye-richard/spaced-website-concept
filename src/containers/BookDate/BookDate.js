import React from 'react';
import Header from '../../components/Header';

const BookDate = ({ gotoLanding }) => (
  <div className="content-wrapper">
    <Header gotoLanding={gotoLanding} />
  </div>
);

export default BookDate;
